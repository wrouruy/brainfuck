# How to Install?
1. clone repo: ``` git clone https://github.com/wrouruy/brainfuck && cd brainfuck ```
2. compile code: ``` gcc main.c -o bf ```
3. make compiled file global(optional): ``` sudo ln -s $PWD/bf /usr/local/bin/bf ```

All done! use it like
```
$ bf <file.bf>
```
